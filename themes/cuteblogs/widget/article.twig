<div class="row mb-5 mt-1 p-3">
<article class="card p-1">
<header>
<img class="img-fluid" width="100%" alt="{{meta.title}}" src="{{ meta.img }}"/>
</header>
<main class="p-3 p-md-5">
<h1><strong><a href="{{ page.url }}"></strong>{{ meta.title }}</a> <span class="cute">✏️</span></strong></h1>
<span>{{ meta.tagline }}</span>
<h3>{{ meta.description }}</h3>
<p class="date">👧&nbsp; {{ meta.author }} ⏰ &nbsp; {{ meta.date }}</p>
<p class="dotted"></p>
{{ content }}
</main>
</article>
</div>